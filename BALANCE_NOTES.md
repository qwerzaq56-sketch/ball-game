# 밸런스 노트 — Version 0.5

이전 버전 노트는 각 폴더에 그대로 있다([v0.1](../v0.1/BALANCE_NOTES.md),
[v0.2](../v0.2/BALANCE_NOTES.md), [v0.3](../v0.3/BALANCE_NOTES.md),
[v0.4](../v0.4/BALANCE_NOTES.md)). 이 문서는 v0.5에서 새로 추가/재설계된 시스템의 실측
결과다. (여느 때처럼 Browser pane이 숨겨지면 `requestAnimationFrame`이 멈추므로,
`game.update(dt)`를 고정 타임스텝으로 직접 호출하며 검증했다.)

## [추가 수정] 출시 직후 발견: 적(AI)이 공격을 거의 무한정 남발하던 버그

사용자 리포트("적 공이 공격을 무한으로 쓰는데")로 발견. 원인은 `updateAttackStack()`/
`updateDodgeStack()`(스택 회복 타이머)이 **AI에 한해 프레임마다 두 번씩 호출되고
있었다** — `js/ai.js#updateAI()` 안에서 한 번, 그리고 `js/game.js#update()`의 Player+AI
공용 루프에서 또 한 번. 즉 AI의 스택 회복 타이머만 매 프레임 `dt`가 아니라 사실상 `2×dt`씩
누적되어, 설정된 `attackCooldown`(1.5초)의 절반도 안 되는 시간마다 스택이 다시 차고
있었다.

격리된 1:1 전투 테스트로 직접 재현: 고정 위치의 AI 두 마리를 붙여놓고 연속 공격 간격을
측정한 결과, 수정 전에는 첫 공격(t=0.567s)과 두 번째 공격(t=1.467s) 사이 간격이 **0.9초**
— 설정된 쿨다운(1.5초)보다 명백히 짧았다. `js/ai.js`에서 중복 호출을 제거하자 간격이
**1.517초**로 정상화되어 설정값과 정확히 일치했다.

플레이어는 애초에 `game.js`의 공용 루프에서만 호출됐기 때문에 이 버그의 영향을 받지 않았다
— AI만 겪는 문제였고, 그래서 "내(플레이어) 공격/회피는 정상인데 적만 이상하다"는 형태로
나타났다.

### 조치

`js/ai.js#updateAI()`에서 중복 호출 두 줄을 제거하고, `js/game.js`의 공용 루프(Player+AI
모두를 한 번씩 순회하며 넉백/체력리젠/스택회복을 처리하는 곳)만 스택 회복의 유일한 호출
지점으로 남겼다. 같은 실수가 재발하지 않도록 `ai.js`에 "스택 회복은 여기서 호출하지 않는다"는
주석을 남겨뒀다.

이런 종류의 버그(같은 업데이트 함수가 두 군데서 호출되어 효과가 배가되는 것)는 콘솔에
에러를 전혀 남기지 않기 때문에, 매 프레임 상태 변화를 직접 로깅해 간격을 재는 것 외에는
발견하기 어려웠다 — 이번처럼 "숫자가 이상하게 빠르다/느리다"는 리포트를 받으면 단위
테스트보다 먼저 "혹시 같은 업데이트가 중복 호출되고 있지 않은가"를 의심해볼 가치가 있다.

## 이번 버전의 접근: 근본적으로 다시 설계해서 버그 클래스 자체를 없앰

v0.4 BALANCE_NOTES에서 "흡수 시스템처럼 두 힘이 맞서는 구조는 중간 지점에서 안정적인 평형이
생기지 않는지 반드시 확인해야 한다"고 적었다. v0.5는 기획안이 요구한 "거리 기반 흡수"로의
전환을 계기로, 아예 **위치를 억지로 잡아당기는 물리 자체를 제거**했다 — 흡수 진행 속도는
오직 "현재 거리"라는 순수 함수로만 결정되고, 대상의 위치를 조작하는 코드는 아주 약한 장식용
당김(`pullForce`, 진행에는 전혀 영향 없음)뿐이다. 두 힘이 서로 맞서 균형을 이룰 조건 자체가
사라졌으므로, v0.3/v0.4에서 두 번이나 발견됐던 "교착 상태" 버그 클래스가 구조적으로 재발할 수
없다. (이 판단 자체가 이번 버전의 가장 중요한 설계 결정이라 GAME_DESIGN.md에도 기록했다.)

## §1-3 흡수 시스템(거리 기반) — 검증 결과

- **겹치지 않아도 흡수 시작**: 흡수자 기준 `maintainDistance`(Size 비례)의 50% 거리(약
  70px, 서로 전혀 닿지 않은 상태)에서 흡수가 정상적으로 시작되는 것을 확인.
- **거리 비례 속도**: 같은 시간(0.5초) 동안 쌓이는 진행도를 거리별로 측정 — 거리 0에서
  0.259, 30%거리에서 0.205, 60%거리에서 거의 0으로 뚜렷하게 감소하는 것을 확인(멀수록
  느려짐 요구사항 충족).
- **범위 이탈 시 연결 즉시 해제**: `maintainDistance`의 130%까지 떨어뜨리자 즉시 취소되고
  대상이 생존하는 것을 확인.
- **저항 시간 공식**: `baseResistanceTime(0.2) + targetSize×resistancePerSize(0.02)`가
  정확히 반영됨(예: size 35 대상 → required ≈ 1.10초, 계산값과 일치).

## §4-6 Size 기반 공격력 / 방어력 / 사거리

- **공격력**: `damage = baseAttackDamage(10) + size×attackDamagePerSize(1)` — Size 20/40/80
  에서 30/50/90으로 정확히 일치.
- **방어력**: `defense = size×0.5` — Size 40 대상은 20 방어, raw 30 피해가 최종 10 피해로
  감소하는 것을 `applyDefense()` 단위 테스트로 확인.
- **기본 공격 범위 120**: `attackRangeForSize(40, balance)`가 정확히 120.0을 반환 — Size가
  `referenceSize`(40)와 같을 때 `baseAttackRange`가 그대로 나오는 것이 공식의 정의이므로
  일치가 당연하지만, 리팩터링 과정에서 깨지지 않았음을 확인하는 차원에서 기록.

## §6 공격 범위 ↔ 돌진 거리 버그 수정 — 검증 결과

기존 버그: `js/combat.js`의 CHARGING 단계가 `attackChargeSpeed`(고정 650px/s)×
`attackChargeDuration`(고정 0.25s) = **항상 162.5px**로 돌진 거리가 고정되어 있었다 — Size가
아무리 커져 `currentAttackRange`가 늘어나도 실제 돌진 거리는 전혀 변하지 않았다.

수정: 돌진 "속도"를 `currentChargeDistance / attackChargeDuration`으로 역산해서, 항상
정확히 `currentChargeDistance`(= 공격 범위 × `chargeDistanceMultiplier`)만큼 이동하도록
변경. 실측: growth를 크게 올려 Size 133.1로 만든 뒤 텔레그래프+차지를 모두 재생시킨 결과,
예상 돌진 거리 508.0px에 대해 실제 이동 거리 501.2px(약 1.3% 오차, 프레임 이산화로 인한
정상 오차 범위) — 공격 범위가 커질수록 실제로 더 멀리 돌진하는 것을 확인.

## §7-9, §25 공격/회피 스택 시스템

- **해금 임계값**: Size 20/39/40/49/50/69/70/99/100 전 구간에서 `attackMaxStack`/
  `dodgeMaxStack`을 스윕 테스트한 결과, 기획안 §9 표와 **정확히 일치**(40→공격1, 50→회피1,
  70→회피2, 100→공격2).
- **연속 사용**: 2스택 상태에서 공격을 텔레그래프→차지→회복까지 완주시킨 뒤 곧바로 두 번째
  공격을 시작 — 쿨다운 대기 없이 즉시 발동되는 것을 확인(스택 2→1→0).
- **회복**: 스택이 0인 상태로 `attackCooldown`(1.5초)만큼 대기하자 정확히 1스택 회복,
  최대치를 넘지 않는 것을 확인.
- **해금 순간 즉시 사용 가능**: `addGrowth()`로 Size를 한 번에 216.8까지 올리자
  `attackStack`이 즉시 2(가득 참)로 설정됨을 확인 — "막 해금된 능력을 처음부터 충전해서
  기다려야 하는" 어색함을 피했다.

## §17 적 최대 크기 스케일링

`spawnAI()`를 각 플레이어 Size(20/100/300)에서 1500회씩 호출해 관측된 최대 크기를 측정:
20→55.8, 100→120.0, 300→279.5. 공식(`40 + playerSize×0.8`)의 이론값(56/120/280)과 거의
정확히 일치(표본 오차 수준). 동시에 `smallSizeMin/Max`는 플레이어 Size와 무관하게 고정되어
있어, 성장한 이후에도 여전히 쉬운 소형 개체가 계속 스폰되는 것을 코드 검토로 확인(§17-3 "모든
적이 플레이어보다 크게 생성되는 것은 아님" 요구사항).

## §18 다른 색상 물리적 밀어내기

- 완전히 겹친(거리 0) 서로 다른 색 AI 두 마리(Size 50/30)에 `resolvePushApart()`를 한 번
  적용하자 거리가 정확히 최소 분리 거리(40 = 25+15)로 즉시 분리되는 것을 확인.
- **같은 색상 쌍은 영향받지 않음**(여전히 완전히 겹친 상태 유지) — 같은 색은 흡수 시스템을
  통해서만 상호작용해야 한다는 요구사항과 일치.
- **CHARGING 중인 개체는 그대로 통과**(밀려나지 않음) — 공격 돌진과 일반 이동 충돌이 서로
  간섭하지 않아야 한다는 §18-2 요구사항을 정확히 만족.

## §1 실행 방식 개선

`run.bat`을 "서버를 백그라운드로 띄우고 2초 대기 후 브라우저를 여는" 구조로 재작성했다.
완벽한 헬스체크(포트가 실제로 열렸는지 폴링)는 배치 스크립트만으로 구현하기 번거로워서
채택하지 않았다 — 대신 고정 2초 지연을 사용했다. 로컬 정적 서버(`python -m http.server`)는
바인딩이 사실상 즉시(수십 ms) 끝나므로 2초는 넉넉한 여유다. 이 부분은 실제 Windows
환경에서의 브라우저 자동 실행 자체는 이 세션(Linux 기반 자동화 환경)에서 직접 실행해
확인할 수 없었다 — 실제 Windows PC에서 최종 확인이 필요하다(아래 "확인 필요" 참고).

## 확인이 필요한 항목 (이 자동화 세션에서 직접 검증 불가)

- `run.bat` 더블클릭 시 실제로 기본 브라우저가 자동으로 열리는지는 Windows 환경에서
  직접 실행해봐야 한다. 배치 파일 문법 자체는 검토했지만 실제 실행 확인은 못했다.
- 사운드는 이번에도(v0.3부터 동일) Web Audio 노드 그래프 구성에 예외가 없는 것만 확인했고,
  실제 청취 확인은 자동화 브라우저 환경의 한계로 불가능했다.

## 추가로 조정이 필요할 수 있는 수치

- 흡수 저항 시간이 v0.4(0.8~수 초)에서 v0.5(0.2~1초대)로 대폭 짧아졌다 — 의도적으로 "여러
  번의 짧고 잦은 흡수 시도"를 유도하는 방향이지만, 실제 플레이에서 너무 시시하게 느껴지면
  `resistancePerSize`를 0.03~0.05로 올리는 것을 고려할 수 있다.
- `enemyMaxSizePerPlayerSize`(0.8)는 성장이 빠른 후반부에 적 크기가 매우 가파르게 커진다
  (Size 300일 때 최대 적 크기 ~280). 후반 난이도가 과하게 느껴지면 0.5~0.6으로 낮추는 것을
  고려할 수 있다.
- Defense 공식이 매우 단순한 선형(`size×0.5`)이라, 공격력도 선형(`size×1`)으로 스케일하는
  것과 맞물려 "거의 같은 크기끼리는 어차피 비슷하게 티격태격한다"는 균형이 나온다 — 이것이
  의도한 긴장감인지, 아니면 사이즈 차이가 실제 승패를 더 명확히 가르길 원하는지는 다음 QA
  라운드에서 확인이 필요하다.
